apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: l06p01-probes-app
  namespace: openshift-gitops
spec:
  generators:
  - list:
      elements:
      - city: tokyo
      - city: paris
      - city: londres
      - city: rome
      - city: sydney
      - city: rio
      - city: istanbul
      - city: berlin
      - city: nairobi
      - city: madrid
  template:
    metadata:
      name: 'l06p01-{{city}}'
    spec:
      project: formation-openshift
      source:
        repoURL: https://github.com/neutron-IT-organization/formation-openshift-gitops.git
        targetRevision: main
        path: gitops/l06p01/base
      destination:
        server: https://kubernetes.default.svc
        namespace: '{{city}}-user-ns'
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=false
