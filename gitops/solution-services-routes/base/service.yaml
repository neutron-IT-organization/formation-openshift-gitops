apiVersion: v1
kind: Service
metadata:
  name: olympic-medals-service
  labels:
    app: olympic-medals-app
spec:
  selector:
    app: olympic-medals-app
  ports:
    - name: http
      port: 5000
      targetPort: 5000
  type: ClusterIP
