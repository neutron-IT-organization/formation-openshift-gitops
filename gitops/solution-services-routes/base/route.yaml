apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: welcome-route
spec:
  to:
    kind: Service
    name: welcome-svc
  port:
    targetPort: 80
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
